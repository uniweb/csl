/**
 * Create a citation registry for a compiled style.
 *
 * @param {import('@citestyle/types').CompiledStyle} style - A compiled CSL style module
 * @returns {Registry}
 */
export function createRegistry(style) {
  throw new Error('Not yet implemented')
}
