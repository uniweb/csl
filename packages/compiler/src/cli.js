#!/usr/bin/env node

/**
 * CLI entry point.
 *
 * Usage:
 *   citestyle compile apa.csl -o apa.js
 *   citestyle compile apa.csl --locale en-US -o apa.js
 */

console.error('@citestyle/compiler CLI — not yet implemented')
process.exit(1)
