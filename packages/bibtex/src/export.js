/**
 * Export CSL-JSON items as a BibTeX string.
 *
 * @param {import('@citestyle/types').CslItem[]} items
 * @returns {string} BibTeX source
 */
export function exportBibtex(items) {
  throw new Error('Not yet implemented')
}
